# Tasks - CoinGap

## Tasks

- [ ] Implement `KlinesSyncService` startup backfill + cron sync (BL-44 tasks 2.2.1 & 2.2.2): `onModuleInit()` fetch 7-day 1h klines for top 20 coins, `@Cron("*/5 * * * *")` sync latest, advisory lock, `skipDuplicates` upsert — register in `klines.module.ts`, add `KLINE_SYNC_ENABLED` to `env.config.ts`
- [ ] Implement on-demand fetch in `KlinesService.getKlines()` (BL-44 task 2.2.3): inject `KlinesSyncService`, call `fetchOnDemand()` when DB result is empty, upsert including current open candle
- [ ] Fix unsafe `as` type casts in klines fetchers (plan: `smooth-bubbling-sutherland.md`): add `isKlineExchange`, `isBinanceErrorResponse`, `isOkxApiResponse`, `isBybitApiResponse` type guards — 4 files affected
- [ ] Vercel Deployment Prep — add `export const dynamic = "force-dynamic"` to `coins/[symbol]/page.tsx`, create `.env.example` for both packages, audit `env.ts` defaults (BL-55)
- [ ] Fix remaining MEDIUM issues from Frontend JSX/TSX analysis (BL-45): 11 medium issues across components, hooks, pages — run `pnpm --filter coingap-interface type-check` + lint after each fix

## Notes

- `klines-sync.service.ts` already created (untracked) — check current state before starting task 1
- BL-44 tasks 2.2.1–2.2.3 must be done sequentially (dependency chain)
- Type guard fixes (task 3) are self-contained — can be done in parallel with task 4
- BL-55 is marked critical — prioritize if time is short
